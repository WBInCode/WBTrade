Kontynuuję naukę aplikacji WBTrade (e-commerce w React + Express + Prisma).

UKOŃCZONE:
✅ Etap 1: Struktura projektu (monorepo, package.json, docker-compose)
✅ Etap 2: Baza danych (Prisma schema, modele, enum vs model)
✅ Etap 3: React podstawy:
   - Komponenty i JSX
   - Props (interface, destrukturyzacja)
   - useState (stan, settery, spread operator)
   - useEffect (pobieranie danych, tablica zależności)

NASTĘPNE:
⏳ Etap 4: Next.js (routing, SSR, API routes)
⏳ Etap 5: Backend Express (kontrolery, serwisy, middleware)
⏳ Etap 6: Analiza prawdziwych komponentów z WBTrade